services:
  web:
    image: node:22-alpine
    container_name: sardauna-site
    restart: unless-stopped
    working_dir: /app
    command: node server.js
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - NODE_ENV=production
    volumes:
      - ./app:/app
